import 'package:flutter/material.dart';

class RentRequest extends StatelessWidget {
  const RentRequest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'RentRequest',
        style: TextStyle(fontSize: 30),
      ),
    );
  }
}