import 'package:flutter/material.dart';

class RentOffer extends StatelessWidget {
  const RentOffer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'RentOffer',
        style: TextStyle(fontSize: 30),
      ),
    );
  }
}